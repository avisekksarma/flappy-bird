#ifndef OBSTACLE_HPP
#define OBSTACLE_HPP

#include <SFML/Graphics.hpp>

class Obstacle : public sf::Transformable, public sf::Drawable
{
public:
    
private:
    void draw(sf::RenderTarget &target, sf::RenderStates states) const{

    }
};

#endif
